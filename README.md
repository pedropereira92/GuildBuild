# Ficha de build — espelho publicado

Conteúdo **gerado**. A fonte fica no repositório privado do bot, em `web/`, e é
publicada aqui por `scripts/publicar-pagina.sh`.

Não edite nada por aqui: a próxima publicação sobrescreve.
